apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-inbound-agent.fullname" . }}-test-connection"
  labels:
    {{- include "jenkins-inbound-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkins-inbound-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

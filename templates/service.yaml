apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-inbound-agent.fullname" . }}
  labels:
    {{- include "jenkins-inbound-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-inbound-agent.selectorLabels" . | nindent 4 }}
